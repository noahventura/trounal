rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /screenshots/{userId}/{tradeId} {
      // Users can only read/write their own screenshots
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
